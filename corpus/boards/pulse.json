{
  "name": "pulse",
  "description": "One-shot UserInput Pulse feeding a DELAY, so the one-tick assertion is observable on both the source link and its consumer over time.",
  "ticks": 7,
  "triggers": [{ "tick": 0, "comp": 0, "event": "pulse", "state": [true] }],
  "board": {
    "links": 2,
    "components": [
      { "type": 200, "inputs": [], "outputs": [0] },
      { "type": 5, "inputs": [0], "outputs": [1] }
    ]
  }
}
