# Benchmark results

Recorded numbers for the performance-improvement phases (perf plan P0+). One section per phase;
each records before/after/Δ per board against this protocol. The boards are generated by
`corpus/tools/gen-bench.mjs` (deterministic — regenerating reproduces the files byte-for-byte);
what each board measures is in its own `description` field.

## Protocol

```
taskset -c 0,2,4,6,8,10,12,14 ./target/release/sim bench corpus/bench/<board>.json \
    --ticks <N> --repeat 10 [--threads T --par-threshold P]
```

- **Machine**: i7-13700K (8 P-cores + 8 E-cores), Linux (Fedora), `intel_pstate/powersave`
  (boost active). Pinned to the 8 *physical* P-cores — one HT sibling each, no E-cores — so
  numbers don't depend on where the scheduler lands a worker.
- **Idle machine**: check `top` before running (an indexing IDE costs double-digit percent).
- The **best** ticks/s of 10 repeats is the recorded figure (least noise); `--ticks` sized per
  board so one repeat takes ~1–4 s (values below).
- Bindings: `corpus/tools/bench-node.mjs` (Node addon, `runAsync`, release build),
  `corpus/tools/bench-wasm.mjs` (wasm blocking `run()`, nodejs-target release build), and
  `corpus/tools/bench-cpp-node.mjs` (the **old C++ engine**, `@logigator/logigator-simulation`
  1.2.1 — the rewrite's reference point).
- Acceptance per phase: the stated target boards improve by the stated hypothesis, and no board
  regresses by more than ~2% (the noise floor; re-run before concluding a real regression).

Per-board `--ticks` (one repeat ≈ 1–4 s single-threaded):

| board | ticks |
|---|---|
| small_idle | 40_000_000 |
| small_active | 4_000_000 |
| medium_idle | 16_000_000 |
| medium_active | 250_000 |
| large_idle | 16_000_000 |
| large_active | 3_000 |
| fanout | 20_000 |
| correlated | 350_000 |

## P0 — Baseline (commit 906d84e, rustc 1.96.0, Node v24.16.0)

Native CLI (`sim bench`, best of 10), ticks/s:

| board | ST | MT 4 threads | MT 8 threads | notes |
|---|---:|---:|---:|---|
| small_idle | 33_818_920 | 32_762_269 | 33_173_025 | frontier ≈ 0; driver never engages |
| small_active | 3_090_935 | 2_939_720 | 2_986_437 | frontier 50 < 2048 → ST path |
| medium_idle | 13_336_633 | 12_988_056 | 12_973_046 | frontier ≈ 9 → ST path |
| medium_active | 173_939 | 164_726 | 166_601 | frontier 1000 < 2048 → ST path |
| large_idle | 13_582_831 | 12_958_723 | 12_957_712 | frontier ≈ 9 → ST path |
| large_active | 859 | 706 | 947 | parallel engages (frontier 200k) |
| fanout | 16_183 | 11_036 | 13_483 | parallel engages (frontier 10k) |
| correlated | 247_837 | 259_256 | 258_431 | frontier < 2048 → ST path |

Reading the baseline:

- **Parallelism is currently a net loss or a wash.** `fanout` *regresses* 17–32% when the
  parallel driver engages, and `large_active` gains only ~10% at 8 threads (and loses at 4).
  This is the headline P4 target (per-tick allocation churn, per-`run` pool rebuild, per-type
  join barriers) — and until P4 lands, the recorded MT "wins" to beat are these numbers, not ST.
- **Boards that stay on the ST path pay a small threads-enabled tax** (~2–5% on `medium_active`):
  `run_parallel` uses the `COUNT = true` read phase (frontier bookkeeping + threshold check) and
  rebuilds the rayon pool per `run()`. The plan's "no small-board regression" property holds
  approximately, not exactly — worth re-checking after P4.
- **Idle cost is activity-proportional** as designed: `large_idle` (200k components) ticks as
  fast as `medium_idle` (1k). The ~2.5× gap to `small_idle` is the active corner (9 links/tick
  vs ~1), not board size.
- **Per-component cost on fully-active boards** is ~6 ns/compute, flat across three orders of
  magnitude of working set: small 3.09M ticks/s × 50 gates ≈ 155M, medium 174k × 1000 ≈ 174M,
  large 859 × 200k ≈ 172M, fanout 16.2k × 10_004 ≈ 162M gate-computes/s. The per-tick loop, not
  memory, is the bound — which is exactly what P2/P6 (read-phase restructure) go after.

### Node binding (`bench-node.mjs`, release addon, `runAsync`, best of 10)

| board | ST ticks/s | vs native CLI ST |
|---|---:|---:|
| small_idle | 55_080_279 | **+63%** |
| small_active | 3_070_294 | -1% |
| medium_idle | 15_424_856 | +16% |
| medium_active | 174_432 | ~0% |
| large_idle | 15_516_600 | +14% |
| large_active | 869 | +1% |
| fanout | 15_961 | -1% |
| correlated | 264_620 | +7% |
| large_active (8 threads) | 952 | ≈ CLI MT8 (947) |

The Node binding *beating* the native CLI on idle boards is P1's hypothesis confirmed
cross-surface before P1 exists: the Node worker reads the clock once per 4096-tick batch, while
`run_single` calls `update_speed` → `clock_gettime` every tick. On idle boards the per-tick clock
read dominates (+63% on `small_idle`); on active boards tick work dominates and the surfaces
agree to within noise. P1 should bring the CLI/core numbers up to (or past) the Node ones.

### Old C++ engine (`bench-cpp-node.mjs`, `@logigator/logigator-simulation` 1.2.1, best of 10)

The reference point the rewrite is measured against. Same protocol, same boards, synchronized
`start()`; repeats continue from the running board state (the singleton can't be re-inited — see
the script header) — irrelevant for these steady-state boards.

| board | C++ ST ticks/s | Rust native ST | Rust vs C++ |
|---|---:|---:|---:|
| small_idle | 32_829_258 | 33_818_920 | +3% |
| small_active | 2_805_278 | 3_090_935 | +10% |
| medium_idle | 12_386_724 | 13_336_633 | +8% |
| medium_active | 125_520 | 173_939 | **+39%** |
| large_idle | 12_368_967 | 13_582_831 | +10% |
| large_active (300 ticks/repeat) | 235 | 859 | **+266%** |
| fanout | 10_884 | 16_183 | **+49%** |
| correlated | 275_721 | 247_837 | **-10%** |

- The Rust engine wins everywhere except `correlated`, where the C++ engine is ~10% *faster*.
  `correlated` is the duplicate-compute board (multi-input components recomputed once per
  flipped input on the Rust ST path) — consistent with P3's thesis; re-check this row after P3.
- The C++ engine's multithreading is not a usable baseline: `start(threads: 8)` **segfaults** on
  `large_active`, silently runs nothing at `threads: 2`, and on `small_active` (where it does
  run) is ~8× *slower* than its own ST (335_607 vs 2_805_278 ticks/s). ST is the only honest
  C++ column.

### WASM spot-check (`bench-wasm.mjs`, blocking `run()`, Node-hosted, best of 10)

| board | wasm ticks/s | vs native CLI ST |
|---|---:|---:|
| small_active | 2_311_669 (4M ticks) | -25% |
| small_idle | 15_635_228 (20M ticks) | **-54%** |

`small_active`'s -25% is the expected wasm-vs-native tax. `small_idle`'s -54% is the P1 target:
the blocking `run()` pays a `performance.now()` call (through `web-time`) every tick, and on an
idle board that read dominates the tick itself. P1's hypothesis ("multiples on wasm blocking
run") predicts this number lands near the active-board tax once the clock read is amortized.

### Hotspot profiles (`perf record --call-graph dwarf`, release + debuginfo)

Reference for "did P2/P6 remove what we thought". Reproduce with:

```
CARGO_PROFILE_RELEASE_DEBUG=true cargo build -p sim-cli --release
taskset -c 4 perf record --call-graph dwarf ./target/release/sim bench corpus/bench/<board>.json --ticks <N> --repeat 1
```

Flat profile, nearly identical on both boards (large_active / fanout):

| symbol | large_active | fanout | what it is |
|---|---:|---:|---|
| `run_tick` | 47.3% | 51.0% | read phase + queue bookkeeping |
| `TickCtx::set_output` | 31.2% | 30.9% | output flip → driver_count + write_buf |
| `dispatch_compute` | 18.0% | 17.6% | the kernels themselves (NOT here) |

Inside `run_tick` (inlined attribution): `read_phase` self 23.3% / 14.4%, `BitSet::get` 5.9%
(large), **`link_consumers` 3.1% / 7.7%** — the per-iteration re-borrow is directly visible, worst
on fanout exactly as P2 predicts — plus `compute_queue` `Vec::push` 6.0% (fanout). Inside
`set_output`: `write_buf` `Vec::push` 6.3%, `link_state`-adjacent `BitSet` get/set 3.9%/3.2%,
`driver_count` atomic store 2.6% (large_active) — the traffic P6 deletes for fan-in-1 links.

Takeaway: ~78% of an active tick is read-phase + `set_output` plumbing around ~18% of actual
kernel work. P2 (consumer groups) attacks the enqueue third, P6 (word-level read + single-driver
links) attacks both sides of the remaining plumbing.

### Decision (2026-06-11): remove the adaptive parallel driver (plan P1.5; P4 dropped)

The baseline made MT's position clear: a net loss on `fanout` (-17…-32%), +10% on
`large_active` at 8 threads, and unreachable for the most common board size (`medium_*` never
crosses `par_threshold`). A follow-up MT8 profile of `large_active`
(`perf record --call-graph dwarf ... --threads 8`, 51k samples ≈ 5.5 cores busy for that +10% —
~20% parallel efficiency) attributes the time to:

| where MT8 time goes | share | fixable by P4? |
|---|---:|---|
| `TickCtx::set_output` — atomic `driver_count` RMW + bitset `fetch_or`, cross-core line ping-pong | 35.9% | no |
| rayon worker loop (useful read-phase work + bridge/steal overhead) | 36.2% | marginally |
| `run_tick_adaptive` self — serial merge + per-tick orchestration | 13.5% | no (serial) |
| kernels (`dispatch_compute`) | 8.4% | — |
| `memmove` — the allocation/copy churn P4 targeted | **2.1%** | yes, but it's 2% |

P4's premise (churn-dominated MT cost) is falsified; the loss is structural and its real fix is
P6's word-ownership read phase. Since MT's only beneficiary is 100k+-component fully-active
boards on native/Node (wasm is ST-only; the old C++ engine's MT segfaults, so no user-visible
capability regresses), the decision is to **delete the driver** (plan P1.5) rather than tune it.
From P1.5 on, this file's tables are ST-only plus the binding columns; the MT columns in the P0
tables above are the historical record. If parallelism is revisited post-P6, start from the
word-ownership design note kept in the plan's P6 §6.

### Pending

- Browser-host wasm numbers (the spot-check above is Node-hosted; same engine family, but
  re-measure in a real browser if a phase decision ever hinges on the wasm column).